{
  "$schema": "../../../../schemas/pack.schema.json",
  "schemaVersion": "1.0",
  "id": "auth-rbac",
  "kind": "capability",
  "name": "Auth + RBAC/ownership",
  "description": "Adds session authentication plus role-based access control and per-record ownership checks on top of an existing stack pack.",
  "version": "1.0.0",
  "inputs": [
    { "name": "sessionStrategy", "type": "enum", "required": true, "options": ["jwt", "database-session"], "description": "How sessions are issued and verified." },
    { "name": "roles", "type": "string", "required": false, "default": "admin,member", "description": "Comma-separated list of role names to seed." }
  ],
  "generatedArtifacts": [
    { "path": "src/lib/auth.ts", "description": "Session verification and role/ownership guard helpers." },
    { "path": "src/middleware.ts", "description": "Route-level authentication middleware." },
    { "path": "prisma/migrations/0002_add_roles/migration.sql", "description": "Adds role and ownership columns to existing tables." }
  ],
  "compatibility": {
    "requiresPacks": ["nextjs-postgres"],
    "conflictsWithPacks": [],
    "profiles": ["internal-crud", "saas"]
  },
  "verification": [
    { "id": "auth-tests", "command": "npm test -- auth", "description": "Authenticated, unauthenticated and cross-tenant access attempts are all exercised." }
  ],
  "upgrade": [],
  "operationalEvidence": [
    { "control": "access-control", "evidence": "Every generated API route is wrapped in the ownership/role guard by default, and the test suite asserts a 403 on cross-tenant access." }
  ]
}
