{
  "$schema": "../schemas/pack.schema.json",
  "schemaVersion": "2.0",
  "id": "db",
  "kind": "capability",
  "name": "Database persistence (Prisma + PostgreSQL)",
  "description": "Adds a Prisma-managed PostgreSQL persistence layer to a stack pack that does not already ship one. Not for use with nextjs-postgres, which already includes this.",
  "version": "2.0.0",
  "inputs": [
    {
      "name": "poolSize",
      "type": "number",
      "required": false,
      "default": 10,
      "description": "Maximum Prisma connection pool size."
    }
  ],
  "setup": [
    {
      "id": "install",
      "command": "npm install @prisma/client @prisma/adapter-pg pg && npm install --save-dev prisma",
      "description": "Adds the ORM and its PostgreSQL driver adapter to a stack that does not already ship persistence."
    },
    {
      "id": "init",
      "command": "npx prisma@latest init --datasource-provider postgresql",
      "description": "Prisma's own CLI lays out prisma/ and the datasource block; the app's models are added on top of it."
    }
  ],
  "requiredArtifacts": [
    {
      "path": "prisma/schema.prisma",
      "description": "Base datasource and generator block; the app's own models are added on top."
    },
    {
      "path": "prisma.config.ts",
      "description": "Prisma CLI config — migrations path and datasource url sourced from DATABASE_URL."
    },
    {
      "path": "lib/db.ts",
      "description": "Client singleton wired to the driver adapter."
    }
  ],
  "compatibility": {
    "requiresPacks": [],
    "conflictsWithPacks": [
      "nextjs-postgres"
    ],
    "profiles": [
      "internal-crud",
      "saas"
    ]
  },
  "verification": [
    {
      "id": "migrate-status",
      "command": "npx prisma migrate status",
      "description": "Confirms the schema and the database are in sync."
    }
  ],
  "operationalEvidence": [
    {
      "control": "persistence",
      "evidence": "Prisma migrations under prisma/migrations/ give a repeatable, versioned persistence lifecycle, independent of which stack pack it is layered onto."
    },
    {
      "control": "database-migration",
      "evidence": "prisma migrate deploy is idempotent; every forward migration is expected to ship a hand-written rollback.sql alongside it, per the pattern in reference-apps/nextjs-postgres-crud."
    }
  ]
}
