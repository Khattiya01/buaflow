{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_": "Inlined from expo/tsconfig.base rather than extended. This file is cloned into a consumer workspace with the plugin, where expo/ is not installed, and an extends that cannot resolve breaks every tsconfig-scanning tool in that workspace, not just this app.",
  "compilerOptions": {
    "allowJs": true,
    "esModuleInterop": true,
    "jsx": "react-jsx",
    "lib": ["DOM", "ESNext"],
    "module": "preserve",
    "moduleDetection": "force",
    "moduleResolution": "bundler",
    "customConditions": ["react-native"],
    "noEmit": true,
    "resolveJsonModule": true,
    "skipLibCheck": true,
    "target": "ESNext",
    "strict": true
  },
  "exclude": [
    "node_modules",
    "babel.config.js",
    "metro.config.js",
    "jest.config.js",
    "android",
    "ios"
  ]
}
